@model IEnumerable<SiyayaTravelAssist.Models.Suburb>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SuburbDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SuburbDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SuburbID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SuburbID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SuburbID })
        </td>
    </tr>
}

</table>
