@model IEnumerable<SiyayaTravelAssist.Models.VehicleModelType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VehicleModelTypeDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VehicleModelTypeDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VehicleModelTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.VehicleModelTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VehicleModelTypeID })
        </td>
    </tr>
}

</table>
