@model IEnumerable<SiyayaTravelAssist.Models.AccessLevel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AccessLevelDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AccessLevelDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AccessLevelID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AccessLevelID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AccessLevelID })
        </td>
    </tr>
}

</table>
