@model IEnumerable<SiyayaTravelAssist.Models.VehicleType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VehicleTypeDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VehicleTypeDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VehicleTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.VehicleTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VehicleTypeID })
        </td>
    </tr>
}

</table>
